# 1. 绝对不能上传的环境变量（保护 API Key）
.env

# 2. 绝对不能上传的私密文件（保护 绝密.pdf）
*.pdf

# 3. 忽略本地生成的数据库（体积大，且应该由程序自动生成）
chroma_db/

# 4. 忽略虚拟环境（体积巨大，且只适配你当前的 Mac）
.venv/
venv/

# 5. 忽略 Python 运行时的缓存垃圾文件
__pycache__/
*.pyc
.cursor/